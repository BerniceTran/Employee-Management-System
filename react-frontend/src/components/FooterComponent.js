const FooterComponent = (prop) => {

    return (
        <div>
            <footer className="footer">
                <span className="text-muted">Spring Boot App 2021</span>
            </footer>
        </div>
        
    );
}

export default FooterComponent;